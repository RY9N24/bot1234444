# Telegram Notification Bot

Запуск: установите зависимости через `./install.sh`, укажите токен в `TELEGRAM_TOKEN.txt`, затем запустите `python -m bot.bot_app`.

Подробная документация находится в файле `BOT_DOCS.md`.
